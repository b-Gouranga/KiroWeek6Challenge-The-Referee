# =============================================================================
# The Referee - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to source control - it contains secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# Groq AI Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from https://console.groq.com/
# This is required for the comparison feature to work.
GROQ_API_KEY=your_groq_api_key_here

# Optional: Specify the Groq model to use
# Available models: llama-3.3-70b-versatile, mixtral-8x7b-32768, llama3-70b-8192
# Default: llama-3.3-70b-versatile
GROQ_MODEL=llama-3.3-70b-versatile

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL credentials for the database container.
# These defaults work with docker-compose out of the box.
POSTGRES_USER=user
POSTGRES_PASSWORD=pass
POSTGRES_DB=referee

# Full database connection URL (used by backend service)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://user:pass@db:5432/referee

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# Backend API URL that the frontend will use to make requests.
# In Docker, this should point to localhost since the browser makes the requests.
VITE_API_URL=http://localhost:3000
